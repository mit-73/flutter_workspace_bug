name: package_a
description: "A new Flutter package project."
version: 0.0.1
publish_to: https://my-pub.com

resolution: workspace
environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  package_b:
    hosted: https://my-pub.com
    version: 0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
